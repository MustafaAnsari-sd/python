# it makes the code more readable and concise.
# The walrus operator (:=) allows you to assign a value to a variable as part of an expression.
# It is useful for situations where you want to use a value immediately after assigning it.
# The walrus operator is also known as the assignment expression operator.
# It was introduced in Python 3.8.
# while(data:=input("Enter value: ")):
#     print(data)
#     if data == "m":
#         break
